server:
  port: 8032

dtm:
  use-feign: true

spring:
  application:
    name: dtm-client
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true