spring:
  application:
    name: dtm-banka
  cloud:
    servicecomb:
      discovery:
        version: 1.0.0
        enabled: true
        address: http://${cse_ip}:30100
        watch: false
        app-name: dtm-demo
        service-name: ${spring.application.name}