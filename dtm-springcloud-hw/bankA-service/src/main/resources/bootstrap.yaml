spring:
  application:
    name: dtm-banka
  cloud:
    servicecomb:
      discovery:
        version: 1.0.0
        enabled: true
        address: ${cse_address}
        watch: false
        app-name: dtm-demo
        service-name: ${spring.application.name}